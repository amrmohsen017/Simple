@model IEnumerable<PM.Models.attachemnt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.attachment_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.attachment_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.attachment_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.attachment_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.attachment_id })
        </td>
    </tr>
}

</table>
